{
  "name": "@weather-and-traffic-api/data",
  "version": "1.0.0",
  "main": "dist/index.js",
  "license": "UNLICENSED",
  "private": true,
  "scripts": {
    "build": "rm -rf dist && tsc",
    "test": "jest --coverage",
    "db-migration:make": "knex migrate:make --env production",
    "db-migration:migrate": "knex migrate:latest --env production"
  },
  "dependencies": {
    "knex": "^3.1.0",
    "pg": "^8.11.5"
  }
}
